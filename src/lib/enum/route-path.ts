export enum ROUTE_PATH {
  LOGIN = "/login",
  SIGN_UP = "/signup",
  HOME = "/",
  CONVERSATIONS = "/conversations",
  FILES = "/files",
  PROJECT = "/projects",
  TASK = "/tasks",
  FEEDBACKS = "/feedbacks",
  CALENDAR = "/calendar",
}
